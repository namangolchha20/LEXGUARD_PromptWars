# =============================================================================
# LEXGUARD — Development & test dependencies
# =============================================================================
#
# Install:
#   pip install -r requirements-dev.txt
#
# Includes production requirements plus linting and testing tools.
# =============================================================================

-r requirements.txt

# Dev / test (from root pyproject.toml dependency-groups.dev)
ruff>=0.9.0
mypy>=1.14.0
pytest>=8.3.0
pytest-asyncio>=0.25.0
httpx>=0.28.0
