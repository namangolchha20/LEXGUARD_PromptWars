# =============================================================================
# LEXGUARD — Python dependencies (production / runtime)
# =============================================================================
#
# Python: 3.12+
#
# Install (from repository root):
#   python -m venv .venv
#   .venv\Scripts\activate          # Windows
#   source .venv/bin/activate       # macOS / Linux
#   pip install -U pip
#   pip install -r requirements.txt
#
# Recommended (uses workspace lockfile):
#   uv sync --package lexguard-api
#
# Start API after install:
#   uv run --package lexguard-api lexguard-api
#   # or: lexguard-api  (if venv active and package installed)
#
# Environment: copy .env and set GEMINI_API_KEY
#
# Frontend (Node.js 18+, pnpm 9+ — separate from this file):
#   pnpm install
#   pnpm dev:web
#
# Optional:
#   pip install -r requirements-optional-ocr.txt   # PaddleOCR fallback
#   pip install -r requirements-dev.txt            # tests & linting
#
# Regenerate locked pins from the monorepo:
#   uv export --package lexguard-api --no-hashes -o requirements.txt
# =============================================================================

# --- LEXGUARD workspace packages (editable, install from repo root) ---
-e ./apps/api
-e ./packages/agents
-e ./packages/ai
-e ./packages/benchmark
-e ./packages/ingestion
-e ./packages/orchestrator
-e ./packages/risk
-e ./packages/shared

# --- Third-party runtime dependencies (pinned via uv export) ---
aiofiles==25.1.0
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
certifi==2026.4.22
cffi==2.0.0 ; platform_python_implementation != 'PyPy'
charset-normalizer==3.4.7
click==8.4.0
colorama==0.4.6 ; sys_platform == 'win32'
cryptography==48.0.0
distro==1.9.0
fastapi==0.136.1
google-auth==2.53.0
google-genai==2.3.0
h11==0.16.0
httpcore==1.0.9
httptools==0.7.1
httpx==0.28.1
idna==3.15
lxml==6.1.0
pillow==12.2.0
pyasn1==0.6.3
pyasn1-modules==0.4.2
pycparser==3.0 ; implementation_name != 'PyPy' and platform_python_implementation != 'PyPy'
pydantic==2.13.4
pydantic-core==2.46.4
pydantic-settings==2.14.1
pymupdf==1.27.2.3
python-docx==1.2.0
python-dotenv==1.2.2
python-multipart==0.0.28
pyyaml==6.0.2
requests==2.34.2
sniffio==1.3.1
starlette==1.0.0
tenacity==9.1.4
typing-extensions==4.15.0
typing-inspection==0.4.2
urllib3==2.7.0
uvicorn==0.47.0
uvloop==0.22.1 ; platform_python_implementation != 'PyPy' and sys_platform != 'cygwin' and sys_platform != 'win32'
watchfiles==1.1.1
websockets==16.0
